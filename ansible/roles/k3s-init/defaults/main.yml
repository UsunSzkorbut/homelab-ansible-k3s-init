---
# defaults file for k3s-init

# filepaths
path:
  k3s:
    install: /tmp/k3s_install.sh
    bin: /usr/local/bin/k3s
    conf_src: /etc/rancher/k3s/k3s.yaml
  metallb:
    ipaddresses: /tmp/ipaddresses.yml
    layer2: /tmp/layer2.yml
  kube_dir: /root/.kube/

# namespaces
namespace:
  metallb: metallb-system
  cert: cert-manager
  rancher: cattle-system

# Metallb variables
metallb:
  version: v0.14.8
  ippool: 10.0.0.150-10.0.0.199
  pool_name: first-pool

# Cert Manager variables
cert_manager:
  version: v1.15.2

# Rancher variables
rancher:
  hostname: rancher.homelab.local
  bootstrapPassword: admin
