---
# defaults file for k3s-init

# filepaths
path:
  k3s_install: /tmp/k3s_install.sh
  k3s_bin: /usr/local/bin/k3s
  metallb_ipaddresses: /tmp/ipaddresses.yml
  metallb_layer2: /tmp/layer2.yml

# Metallb variables
metallb:
  version: v0.14.8
  ippool: 10.0.0.150-10.0.0.199
  meta:
    name: first-pool
    namespace: metallb-system

# Cert Manager version
cert_manager_version: v1.15.2